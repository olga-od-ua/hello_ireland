""" A Module for application configuration for the Favourites app """
from django.apps import AppConfig


class FavouritesConfig(AppConfig):
    """ A Class for application configuration for the Favourites app"""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'favourites'
