""" A Module for application configuration for the Home app """
from django.apps import AppConfig


class HomeConfig(AppConfig):
    """ A Class for application configuration for the Home app"""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'home'
